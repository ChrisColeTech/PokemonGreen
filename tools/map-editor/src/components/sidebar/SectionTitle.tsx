function SectionTitle({ children }: { children: string }) {
  return <p className="text-[11px] font-semibold uppercase tracking-[0.06em] text-[var(--text-muted)]">{children}</p>
}

export default SectionTitle
